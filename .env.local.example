# URL of the PocketBase instance that backs this site's content.
# Used both server-side (data fetching) and at build time (next.config.mjs
# image remote patterns) - set it in every environment (local, Vercel, etc).
POCKETBASE_URL=http://127.0.0.1:8090
